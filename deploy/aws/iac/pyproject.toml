[project]
name = "workshield-aws-infrastructure"
version = "0.1.0"
description = "WorkShield AWS CDK application"
requires-python = ">=3.10"
dependencies = [
    "aws-cdk-lib==2.169.0",
    "constructs>=10.0.0,<11.0.0",
]

[tool.uv]
package = false
